<!-- extended javascript -->
<!DOCTYPE html>
<html>
    <head>
        <title>Encontros</title>
        <script type="text/javascript" src="/../js/jquery-1.10.2.min.js"></script>
        <link rel="stylesheet" href="/../css/style.css">
    </head>
<body>
<div class="data-table">
    <table border="1" cellpadding="7" cellspacing="7">
        <tr>
            <!-- <th width="50px">No</th> -->
            <th>Data</th>
            <th>Anfitrião</th>
            <th>Filme</th>
            <th>Endereço</th>
            <th>Participantes</th>
            <th>Action</th>

        </tr>

        <% if(data.length){ 
                        
        for(var i = 0;i < data.length;i++) { 

            if(data[i].validade_filme) {
        %>
        <tr>
            <!-- <td><%=(i+1)%></td> -->
            <td><%=data[i].data_encontro%></td>
            <td><%=data[i].nome%></td>
            <td><%=data[i].nome_filme%></td>
            <td><%=data[i].endereco%></td>
            <td><a href></a></td>
            <td>

                <a class="a-inside delete" href="javascript:void(0)" onClick="deleteEncontro(<%=data[i].id_encontro%>)">Delete</a>

            </td>
        </tr>
    <% }}
    
    }else{ %>
        <tr>
            <td colspan="5">No Data</td>
        </tr>
    <% } %>

    </table>
</div>
<!-- <div class="page-data">
    
    <form method="post" action="" id="the-form">
        <table cellpadding="11">
        <tr>
            <td class="label">Título</td><td>: <input type="text" name="titulo"></td>
        </tr>
        <tr>
            <td class="label">Duração</td><td>: <input type="text" name="duracao"></td>
        </tr>
        <tr>
            <td class="label">Gênero</td><td>: 
                <select id="select1" name="genero">
                    <% for(var i=0; i < data[1].length; i++) { %>
                        <option><%= data[1][i].nome_genero %></option>
                     <% } %>
                </select></td>
         </tr>
         <tr>
            <td class="label"></td>
            <td>
                &nbsp; <input type="button" value="Save" onClick="saveFilme()">
            </td>
         </tr>
        </table>
    </form>
</div> -->
<div class="page-data">
     <ul class="err-area"></ul>
</div>

<script>

    function saveFilme(){
       
       $.ajax({
        
        url:"/api/filme",
        type:"post",
        data:$("#the-form").serialize(),
        success:function(res){
            
            window.location.reload();
            return false;
        },
        error:function(xhr, status, error){
            
            console.log(xhr.responseText);
            var err = '';
            $.each(JSON.parse(xhr.responseText) , function(i, item) {
               
                 err +='<li>'+item.msg+'</li>';
            });
            $(".err-area").html(err);    
            return false;
        }
        
       });
    }
    
    function deleteEncontro(id_encontro){
    
        $.ajax({
            url:"/api/encontro/"+id_encontro,
            type: 'put',
            success: function(res) {
                
                window.location.reload();
                return false;
            },
            error:function(xhr, status, error){
            
                console.log(xhr.responseText);
                alert("Error deleting");
                return false;
            }
        });
    }

</script>
</body>
</html>
